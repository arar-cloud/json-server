{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": {
    "oneOf": [
      { "type": "array" },
      { "type": "object" }
    ]
  },
  "_security": {
    "description": "No authentication or authorization fields defined in schema. API implementations should enforce auth middleware before serving endpoints."
  }
}
